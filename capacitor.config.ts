import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.dyeshop.app',
  appName: 'DyeShop',
  webDir: 'out',
  bundledWebRuntime: false
}

export default config
